# Local stack ports
FRONTEND_PORT=3000
POSTGRES_PORT=5432

# Database settings
POSTGRES_DB=budget
DATABASE_USERNAME=budget
DATABASE_PASSWORD=budget

# Security secrets (JWT secret should be at least 32 characters)
JWT_SECRET=replace-with-a-long-random-string

# ENCRYPTION_SECRET is auto-generated and persisted by backend container
# in the backend_secrets Docker volume. Set it only if you need to
# migrate/restore an existing encrypted SimpleFIN connection dataset.

# Frontend talks to backend through frontend Nginx proxy by default
VITE_API_BASE_URL=/api/v1

# Useful if you call backend directly from browser tools/apps
APP_CORS_ALLOWED_ORIGINS=http://localhost:3000
